package interface_adapter.round;

import view.RoundView;

import javax.swing.*;

public class RoundMain {
    public static void main(String[] args) {
        RoundViewModel roundViewModel = new RoundViewModel();
        System.out.println(roundViewModel);
    }
}